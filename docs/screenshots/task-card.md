# task-card

Capture against solcoach.vercel.app on devnet.
Viewport: 390×844 (iPhone 14) — coach is mobile-first, desktop captures look weird with all that whitespace.

(stub — replace with .png once capture pipeline is set up)
