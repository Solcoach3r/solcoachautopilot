# solcoach chat — tiny anthropic proxy
# (PORT is injected by railway in production; local default = 3099)

# claude api key (server-only, never expose to the frontend)
ANTHROPIC_API_KEY=

# allow your frontend origin in prod, or '*' for fully open
CORS_ORIGIN=*

# local listen port — railway overrides via PORT
PORT=3099
